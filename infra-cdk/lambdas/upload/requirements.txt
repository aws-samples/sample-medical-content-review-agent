aws-lambda-powertools[tracer]>=3.0.0
pydantic>=2.0.0
boto3>=1.34.0
# Reads the pre-approved claims spreadsheet for the /claims/parse route. MIT licensed,
# and the same version the agent already uses, so both parse identically.
openpyxl>=3.1.5
